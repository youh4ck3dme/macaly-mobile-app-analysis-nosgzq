# Convex deployment URL for the Vite client and SSR.
# `npx convex dev` prints this URL and usually writes it to .env.local.
# Do not commit .env.local — copy this file and fill in the real value.
VITE_CONVEX_URL=
